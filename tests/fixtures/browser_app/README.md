# Browser App Fixtures

Local browser E2E tests use deterministic HTML generated by `tests/e2e/harness.py`.

Keep permanent fixture assets here only when they are shared by multiple E2E tests and are too large or noisy to embed in the harness.
